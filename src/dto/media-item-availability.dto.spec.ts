import { MediaItemAvailability.Dto } from './media-item-availability.dto';

describe('MediaItemAvailability.Dto', () => {
  it('should be defined', () => {
    expect(new MediaItemAvailability.Dto()).toBeDefined();
  });
});
